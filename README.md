Production: https://tnmovingwaters.netlify.app

# Tennessee Moving Waters (Astro)

This site is now an Astro static, multi-page build.

## Commands

- `npm run dev` (local dev server)
- `npm run build` (static build to `dist/`)
- `npm run preview` (serve the production build)

## Legacy React App

The previous Create React App implementation is preserved in `legacy-react/` for reference.
